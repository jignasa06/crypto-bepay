import 'package:flutter/cupertino.dart';

class AmountScreen extends StatelessWidget {
  const AmountScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Amount screen"),);
  }
}
